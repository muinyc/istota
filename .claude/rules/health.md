---
paths:
  - "src/istota/health/**"
---

# Health module (`src/istota/health/`)

Body stats, bloodwork panels, biomarker trends, medical history (encounters + diagnoses), immunization tracking, and Garmin Connect integration. On by default; per-user opt-out via `disabled_modules`. Per-user SQLite on **local disk** at `Config.module_db_path(user, "health")` (moved off the FUSE mount for WAL — see "Module DB storage") with stats, panels, biomarkers, biomarker_explainers, biomarker_refs, encounters, diagnoses, diagnosis_encounters, immunizations, immunization_refs, documents, document_links, profile, display settings; user-facing uploads (source PDFs) stay in the workspace on the mount. All measurements stored metric (kg, cm, °C, mmHg, bpm); display layer converts to the user's preferred units. Module package at `src/istota/health/` with `models`, `db`, `units`, `routes`, `workspace`, `_loader`, `_migrate`, `ocr`, `explainer`, `garmin_sync`, `documents`. Bundled `biomarker_refs.json` seeds 60+ canonical markers with sex-specific reference ranges, alias lists, and one-paragraph clinical descriptions; Istota's canonical ranges drive flagging (lab-printed ranges are preserved per-row but not the flagging source). Blood-pressure / resting-HR biomarker rows fan out to `stats` so the unified time series picks them up. Bundled `immunization_refs.json` seeds 19 adult-relevant vaccine schedules; `immunization_explainers.json` provides static clinical detail per vaccine.

OCR pipeline (`ocr.py`): PDF via `pdftotext`/`pypdf` with `pdftoppm` + Tesseract fallback; image via Tesseract. Extracted text plus the canonical marker list goes to the active brain (general role alias) which returns structured JSON; values >10× outside the widest canonical range surface as a likely-OCR-error warning. Optional deps degrade gracefully.

The three extractors (`ocr.py`, `encounter_ocr.py`, `immunization_ocr.py`) build their own `BrainRequest` rather than going through `execute_task`, so each has to ask for the confinement that path would have assembled: `executor.build_model_cli_env` for the env (ISSUE-395), `fs_read_roots=[document]` for `NativeBrain`'s file tools, and `executor.build_daemon_sandbox(config, user_id, extra_ro_binds=[document])` for the two Claude brains, which ignore those roots and take their boundary from bubblewrap (ISSUE-397). The last one is the whole grant on the default deployment: `build_claude_cli_flags` reads a non-empty `allowed_tools` as the signal to run `--dangerously-skip-permissions` with no allowlist, so an unwrapped `allowed_tools=["Read"]` was the CLI's full toolset — `Bash` and `Write` included — running host-side as the daemon user against an uploaded document. The wrap goes on both branches, not only the vision one, and a request that grants `Read` and *cannot* be confined does not run at all — the caller returns `None` and the UI offers manual entry, since falling back to an unconfined call is the exposure itself. `/encounters/extract` and `/immunizations/extract` write their temp copy to `daemon_work_dir(config, ctx.user_id)`, one level below `config.temp_dir`, because the shared root is bound at no path. The default suite can only read the argv; `tests/linux/test_daemon_sandbox_real.py` executes the namespace on a real kernel and is where the claims about it are actually settled.

Biomarker explainer (`explainer.py`): out-of-range marker pages display an educational alert generated by the active brain. Hard guardrails in the prompt (never diagnose, never prescribe, always hedge, JSON-only output); strict response parsing rejects malformed output and falls back to a fixed safe payload so the UI never shows raw model output. Cached per-user per `(name, direction)` in `biomarker_explainers`; fallback responses are NOT cached so later brain availability still produces real content.

Surfaces: FastAPI router at `/istota/api/health/*` (stats CRUD + series + latest; panels CRUD + upload + extract + source streaming; biomarker trend + summary + canonical refs + matrix endpoint + cached explainer; CSV import/export at `/csv/import` (multipart, `on_collision=skip|replace|append`) and `/csv/export` (text/csv stream of confirmed panels in the same layout); encounters CRUD + history summary; diagnoses CRUD + resolve; immunizations CRUD + refs + coverage + parse + bulk + explainer; Garmin status/connect/mfa/sync/disconnect; profile / display settings; dashboard aggregator). SvelteKit pages under `/health/*` — `/stats` (netdata-style grid with sparklines), `/bloodwork` (dates-as-rows × markers-as-columns spreadsheet with category bands and flag-colored cells, plus Import/Export CSV controls), `/bloodwork/panel?id=…` (panel detail + inline-edit table + source preview), `/bloodwork/upload` (drag-and-drop OCR review-and-confirm), `/bloodwork/marker?name=…` (trend chart with out-of-range zones shaded, related-marker chips, clinical description, history table, LLM explainer card), `/health/history` (timeline + conditions list), `/health/history/encounter?id=…` (per-encounter detail), `/health/history/diagnoses` (diagnoses list, with per-condition document modal), `/health/history/import` (encounter import from paperwork), `/health/immunizations` (coverage strip + quick-log), `/health/immunizations/paste` (parser preview), `/health/immunizations/detail` (per-record edit), `/health/immunizations/vaccine` (drill-down + explainer), `/health/immunizations/import` (paste-text + screenshot/PDF upload), `/health/settings` (DOB/height/sex + display preferences + Garmin connect/disconnect), `/health/documents` (every uploaded document, with its associations editable in place). The two formerly-dynamic routes use query params because `adapter-static` can't crawl runtime-only ids (commit 629c2ac).

**Conditions across encounters** (`SCHEMA_VERSION` 4): a condition is seen at as many appointments as it is seen at, so `diagnoses` ↔ `encounters` is a many-to-many over `diagnosis_encounters` (`PRIMARY KEY (diagnosis_id, encounter_id)`). Unlike `document_links` — polymorphic, so it can carry no FK — both columns here point at exactly one table and carry **real** foreign keys with `ON DELETE CASCADE`, which every caller gets because `connect()` sets `PRAGMA foreign_keys = ON`; deleting either end clears the links without a hand-written sweep. `diagnoses.encounter_id` survives as a legacy scalar: still written by `insert_diagnosis` for an old reader, read by nothing current. `_migrate_diagnosis_encounters` is a **backfill only** (the table itself comes from `CREATE TABLE IF NOT EXISTS`), gated on the stored `schema_meta.version < 4` rather than on the join table being empty — the legacy column is deliberately not cleared, so an emptiness gate would resurrect links the user had removed on every subsequent start. The bug that motivated it: `insert_diagnosis(..., reconcile=True)` correctly recognised a repeated condition as one condition and then **returned early**, so an import naming the same condition at three appointments kept only the first — the condition vanished from the later visits and those visits vanished from its history. It now adds a link on the reconciled row. `PUT /diagnoses/{id}` takes `encounter_ids` as a **full replace** (empty list clears) and it wins over the legacy scalar; `POST /diagnoses/{id}/encounters` and `DELETE /diagnoses/{id}/encounters/{eid}` are the single-link verbs, with re-linking a no-op success. `GET /diagnoses` carries `encounter_ids` per row via one chunked `encounter_ids_for_diagnoses` query, so the list costs no N+1. Agent-side the verbs are `link-encounter` / `unlink-encounter` (the `@ref` grammar resolves an encounter created earlier in the same sandboxed batch); sandboxed they defer `link_diagnosis_encounter` / `unlink_diagnosis_encounter`. Web-side the encounter page lists its conditions with an unlink kebab (worded, and styled, as non-destructive — the condition is kept), the conditions list edits the whole set as chips, and the encounter-create form stages conditions and links them after the insert, degrading to a non-fatal warning if a link fails so a saved encounter is never reported as lost.

**Documents** (health-document-attachments spec): the paperwork behind a record. Three import paths used to OCR a PDF and delete it — `/encounters/extract` said so in its own docstring — so a referral letter that produced three encounters and two diagnoses was unrecoverable the moment extraction returned, and an immunization row was an unevidenced claim (proof of immunization is a document people are actually asked to produce, not merely a fact to recall). Two tables in the per-user health DB: `documents` (one row per blob — sanitized `filename`, `mime`, `byte_size`, `content_hash`, `stored_path`, `ocr_text`, `source` ∈ manual|import|agent) and `document_links`, a **polymorphic** `(document_id, entity_type, entity_id)` join over `encounter | diagnosis | immunization` (`db.DOCUMENT_ENTITY_TYPES`, validated in every function that takes one). Many-to-many from the first import, because one PDF routinely produces several encounters _and_ several diagnoses, and a hand-added diagnosis with `encounter_id NULL` must still carry its own paperwork. Panels are deliberately **not** an entity type: they keep `panels.source_file` and `/panels/{id}/source` unchanged rather than eat a live-DB path-rewriting migration for no user-visible gain. `entity_id` carries no FK (it points at one of three tables), so an entity delete clears its links by hand inside `delete_encounter` / `delete_diagnosis` / `delete_immunization`. Both tables are new, so `CREATE TABLE IF NOT EXISTS` inside `executescript(SCHEMA_SQL)` covers an existing DB and no migration function exists — stated in a code comment so its absence doesn't read as an oversight.

Bytes live at `{uploads_dir}/documents/{id}/{stored_name}` — under the same `uploads_dir` root the panel-source route already guards, so one containment rule (`relative_to(uploads_dir)`) covers both, and the tree stays inside the Nextcloud-synced workspace where the user can see and back up their own paperwork. `stored_name` is the _sanitized original_ filename rather than a UUID or `original.bin`, since that workspace is user-facing; the numbered parent directory makes collisions structurally impossible, so sanitisation (`documents.sanitize_document_filename`: `Path(...).name`, then `[^A-Za-z0-9._-]` → `_`, collapse runs, strip leading dots, 100-char stem, `document.bin` fallback) doesn't have to guarantee uniqueness. `store_document` inserts the row _before_ writing the bytes, so a crash between them leaves a row pointing at a missing file (a 404 the readers already handle) rather than an unreferenced invisible blob, and writes via a `.part` sibling + `os.replace` so a torn write is never visible under the final name. `content_hash` (sha256) is UNIQUE and dedups: uploading identical bytes **reuses the row and adds a link** — attaching one discharge summary to both the encounter and the condition it produced is the normal case and should cost one file — with the `IntegrityError` from a concurrent racer resolving to the same outcome. `store_document` writes the bytes _before_ the caller commits (no caller commits mid-function), so the sweep has to reclaim in both directions.

`sweep_orphan_documents` therefore runs two passes and rides `_migrate.ensure_initialised`, collecting abandoned import uploads without a new scheduler job. The **row pass** deletes linkless documents whose `last_touched_at` is older than 24h — _not_ `created_at`, which was the first version and destroyed any document older than the window the instant its last link went, i.e. exactly the detach-then-reattach-elsewhere correction the delay exists to protect. `last_touched_at` is stamped on create, link, unlink, and on a dedup hit (`db.touch_document`), so a month-old discharge summary detached on its way to another encounter gets a fresh window, and a re-uploaded old scan survives its own review screen (dedup returns the existing row, keeping its original `created_at`, so a sweep would otherwise have deleted the id `/bulk` was about to link — failing the import _and_ losing the file). An abandoned upload is still collected: nothing touches it after the insert. The **directory pass** removes `{uploads_dir}/documents/<id>/` directories with no row, mtime-gated on the same window (a document being written right now legitimately has a directory and no committed row yet) — without it a rollback after `store_document` returns strands a blob that no DB-driven enumeration could ever reach. `store_document` also clears its target directory first, since AUTOINCREMENT hands an id back after a rollback and the leftover would otherwise share the directory with the live document and block the `rmdir` on delete.

A dedup hit additionally **heals missing bytes**: if the matched row's file is gone (a manual workspace edit, a failed sync), the bytes are rewritten. Without that, re-uploading the identical file was a silent no-op — the hash matched, the broken row came back, and the document stayed 404 forever while the upload reported success.

Routes on the existing router: `POST /documents` (multipart, optional `entity_type`/`entity_id`), `GET /documents` (all, or scoped to one record — **both branches carry each document's `links` with resolved labels**, ISSUE-423), `GET /documents/{id}` (+ resolved link labels), `GET /documents/{id}/file`, `POST /documents/{id}/links`, `DELETE /documents/{id}/links/{type}/{id}`, `DELETE /documents/{id}`. `_document_to_dict` never emits `stored_path` — the client gets a `url` to stream from. The file route always sets `Content-Disposition: attachment` + `nosniff`, **unlike** `/panels/{id}/source`, which serves inline: a document can be an email attachment the agent filed, i.e. content from outside the trust boundary, and attacker-supplied HTML/SVG served inline on the app's own origin would execute against the session cookie (the same reasoning recorded for web chat's `GET /chat/files`). `<iframe>`/`<img>` render PDFs and images regardless. MIME resolves declared → `mimetypes.guess_type(filename)` → magic-byte sniff (`_sniff_mime`; a saved email attachment routinely has no extension, and refusing a good scan for want of a `.pdf` is a poor answer), then is allowlisted (`_ALLOWED_DOCUMENT_MIMES` — PDF, JPEG/PNG/HEIC/HEIF/WebP/TIFF, plain text → 415 otherwise) and size-capped by `[health] max_document_bytes` (default 25 MiB, `0` = unlimited; Ansible `istota_health_max_document_bytes`) → 413. The three detail GETs gained `documents`; the three list GETs gained `document_count` per row via `document_counts_for_entities` (one chunked query, so a paperclip badge costs no N+1).

The list route's `links` are resolved by `_documents_payload`, which is the same anti-N+1 shape one level up: `db.entity_links_for_documents` is one chunked query for every listed document's links, then `db.entities_by_id` is one query per **entity type present** — so the cost is bounded at four queries whatever the row count, where the per-row readers would be one links query plus one `get_*` per link. Both readers order identically to their singular counterparts, so a list and a detail cannot disagree about what a document is attached to, and `_format_entity_label` is the one spelling of a label that `_entity_label` now delegates to as well. An id with no row is **absent** from `entities_by_id` rather than mapped to None — `document_links.entity_id` carries no FK, so a link can outlive its record — and the label falls back to `"{entity_type} {entity_id}"`, exactly as the single reader did.

**Two degradations there are load-bearing, and both exist because the bulk path could fail where the per-row path never did.** `entity_type` is a plain `TEXT` column with no CHECK, so a value outside `DOCUMENT_ENTITY_TYPES` is storable even though every writer validates — `_documents_payload` therefore filters the types it hands `entities_by_id`, since an unfiltered call raises `ValueError` and 500s the whole listing, and this is the one page that shows a document no other page does. And the type→table and type→converter maps are declared beside `DOCUMENT_ENTITY_TYPES` (`_ENTITY_TABLES`, `_ENTITY_ROW_CONVERTERS`) with an **import-time** check that all three agree, so a fourth entity type added to the tuple alone fails at import rather than as a `KeyError` inside a request. Both bulk readers also dedupe their id list: `entity_links_for_documents` *appends*, so a repeated id landing in two different chunks would list its links twice. The discriminating test is `tests/test_health_routes.py::TestDocumentRoutes::test_list_link_labels_cost_no_query_per_row`, which counts SELECTs against a growing row count: every other assertion in that class passes against a per-row loop, and both naive halves were run as controls and each turned that one test red. `POST /documents` deliberately does **not** carry `links` — it is a create acknowledgement (`created`, `linked`) rather than a description of the row, which is why `HealthDocument.links` is optional in TypeScript.

Import flow: `/encounters/extract` and `/immunizations/extract` now **keep** the upload (`source="import"`, `ocr_text` carried) and return `document_id`; the review screen passes it to `/bulk`, which links it to every row it creates. A storage failure still returns the extracted rows with `document_id: null` and a warning — losing the file is bad, losing both is worse. An unknown `document_id` is a 400 checked _before_ any insert, so a bad id can't leave half an import behind.

Agent access: `attach-document` / `detach-document` / `documents` / `document` on the `health` skill, with a `TYPE:ID` grammar (`_parse_entity_ref`; `encounter:@NAME` also resolves an encounter created earlier in the same sandboxed batch, mirroring `add-panel --ref`). Sandboxed they defer `attach_document` / `detach_document` ops that `_process_deferred_health_ops` replays. **Every writer validates the target exists first** (`db.entity_exists`, which the web route's `_entity_exists` also delegates to): `entity_id` is polymorphic with no FK, so a link to a record that isn't there leaves a document invisible on every page _and_ permanently exempt from the orphan sweep — it does have a link — so `attach_document` raises `UnknownEntity` before writing a byte. The op file is written inside the sandbox, so its `source_path` is untrusted text and the daemon — which is _not_ sandboxed — confines it to the task's deferred dir or the user's base workspace `{mount}/Users/{uid}`. `_resolved_source_path` resolves symlinks and hands back the approved path, and the read uses _that_: resolving twice would leave a window in which a symlink under the workspace is swapped between the check and the read. `register_upload` goes through the same guard (it had none). Both agent paths also read the operator's `max_document_bytes` (`_health_max_document_bytes` / the skill's `_document_max_bytes`) rather than falling through to the library default, so `0` = unlimited applies to an agent-filed document too. The base dir, not merely the bot subdir, because the driving case is an email attachment the executor dropped in `inbox/`. `skill.md`'s "Filing paperwork" section tells the model to attach to the most specific record a document evidences (the immunization, not merely the visit) and that it files files _the user supplied_ — it does not go looking through the workspace for medical documents. Deletion stays a web-UI action: an irreversible destructive action on medical records should need a human click.

Web UI: `DocumentList` / `DocumentCard` in `web/src/lib/components/health/` (3-across card grid with a dashed drop zone as the last cell; kebab with Open / Detach / Delete, both behind `ConfirmDialog`, and the delete confirmation names how many _other_ records it would strip the file from). A Documents section on the encounter page, "Proof of immunization" on the immunization detail page, and — since conditions have no detail page — a paperclip badge plus a kebab entry opening a `Modal` on the conditions list.

`/health/documents` is the fifth tab and the one surface with **no entity** (ISSUE-423): a `table.grid` over every document, with each association as a chip carrying the server's own label and an × that detaches it, a `+ Attach` opening a type-then-record `Modal`, and Open / Delete on a `KebabMenu`. It is where a document with no links at all is visible — nowhere else showed one, and the orphan sweep would eventually take it — so an "N unattached" `Chip` in the header filters to exactly those. A "move" is an attach plus a detach rather than a verb of its own; the modal's description says so, and no third endpoint was added for it. Deliberately **no upload here**: an upload with no entity to attach to is a new question about where unattached documents come from, and the three entity surfaces are still where a document enters. Two error slots, not one — `loadError` replaces the pane (`.center-msg error`), `actionError` sits above a table that is still correct (`.banner error`) — since a failed detach must not take the listing away, and for the same reason a reload after an edit sets `refreshing` (which dims the table) rather than `loading` (which replaces it).

**Four properties of the page are corrections rather than choices, each of which reads fine until you look.** It **walks every list to the end** (`fetchAllPages`, page size 1000, ceiling 20 pages) instead of taking the server's default: `GET /documents` defaults to `limit=200` and `list_documents` orders newest-first, so a single request silently drops the *oldest* documents — precisely the ones the 24h orphan sweep is about to take and the ones this view exists to surface — while the header states a total and the unattached count is derived from the same truncated array. A walk that hits its ceiling sets `truncated`, and the header then says "showing the N most recent" rather than "N on file", because a count of what is loaded presented as a count of the store is a wrong number offered as a fact. `busy` is a **`SvelteSet` of ids, not a scalar**, since each row's buttons are disabled only by its own id and two mutations are therefore concurrently reachable — one scalar means whichever finishes first clears the other's guard (and a plain `Set` in `$state` is not deeply reactive, so the rows would not re-render). Both dialogs hold a document **id and re-derive the record** from `documents`, never the object: `load()` replaces the array wholesale, so a held reference keeps pre-reload `links`, which would let the picker offer an already-attached record — failing quietly, since `link_document` is `INSERT OR IGNORE` and returns `created: false` — and would let the delete confirmation name a record count that no longer holds, which is the one number the user is being asked to act on. The attach path now reports that `created: false` rather than swallowing it. And the filter chip renders while `unattachedOnly` is true even at zero, *and* `load()` clears the flag at zero: attaching the last loose document would otherwise unmount the only control that turns the filter off and leave an empty table behind it, reached by the exact workflow the view is for.

Presentation helpers are in `web/src/lib/health/documents.ts`: `attachedRecordsWarning` (distinct from `otherRecordsWarning`, which stays silent at zero because there the user is deleting *from* a record; here they are deleting the document, so one attached record is worth naming), `entityTypeLabel` and `attachOptions` — whose filter is on the `(entity_type, entity_id)` **pair**, since encounter 1 and diagnosis 1 are two records sharing a number and an id-only filter would hide a condition from the picker because a visit was attached.

CSV import/export (`src/istota/health/csv_io.py`): 3-row header (category banner + `Marker (unit)` headers + reference range) followed by `date, lab, value…` data rows. Aliases (`Hgb` → Hemoglobin, `LDL-C` → LDL, `Na` → Sodium, `D3 Total` → Vitamin_D, etc.) resolve via `biomarker_refs`; unknown markers store under the printed name and land in the matrix's "Other" category. Imported panels are written confirmed (`draft=0`) and the BP/HR fan-out to stats mirrors the OCR confirm flow.

`health` skill (`src/istota/skills/health/`): `log|stats|latest|panels|panel|add-panel|add-biomarker|trend|upload|import-csv|export-csv|summary|settings|set|encounters|encounter|add-encounter|update-encounter|delete-encounter|diagnoses|diagnosis|add-diagnosis|update-diagnosis|resolve-diagnosis|delete-diagnosis|link-encounter|unlink-encounter|history-summary|immunizations|immunization|add-immunization|update-immunization|delete-immunization|vaccine-refs|coverage|import-immunizations|explain-immunization|garmin-status|garmin-sync|garmin-disconnect|documents|document|attach-document|detach-document`. `setup_env` hook injects `HEALTH_DB_PATH` (no-op when the user has opted out via `disabled_modules`). Writes flow through the deferred-op file `task_<id>_health_ops.json` under sandbox; `_process_deferred_health_ops` replays them post-task (including the `import_csv` op, which re-reads the source file from the deferred path on the host side, and the `attach_document` op, whose source path is containment-checked first).

## Health

Body stats, bloodwork panels, biomarker trends, medical history (encounters + diagnoses), immunization tracking, document attachments, and Garmin Connect integration. On by default; per-user opt-out via `disabled_modules`. Per-user SQLite on **local disk** at `Config.module_db_path(user, "health")` (moved off the FUSE mount for WAL — see "Module DB storage"); user-facing uploads (source PDFs, attached documents) stay in the workspace on the mount. All measurements stored metric (kg, cm, °C, mmHg, bpm); the display layer converts to the user's preferred units. Module package at `src/istota/health/` (`models`, `db`, `units`, `routes`, `workspace`, `_loader`, `_migrate`, `ocr`, `explainer`, `garmin_sync`, `documents`, `csv_io`). Bundled `biomarker_refs.json` seeds 60+ canonical markers with sex-specific ranges and drives flagging (lab-printed ranges are preserved per-row but are not the flagging source); `immunization_refs.json` seeds 19 adult-relevant vaccine schedules.

Surfaces: a FastAPI router at `/istota/api/health/*`, SvelteKit pages under `/health/*` (stats grid, bloodwork spreadsheet + panel/marker detail + OCR upload, history timeline, immunizations, settings), and the `health` skill — 44 subcommands whose sandboxed writes flow through the deferred-op file `task_<id>_health_ops.json`, replayed post-task by `_process_deferred_health_ops`. The OCR pipeline (`ocr.py`) and the biomarker explainer (`explainer.py`) both go through the active brain with hard prompt guardrails, strict response parsing, and fixed safe fallbacks so the UI never shows raw model output.

Full reference — the full schema, conditions-across-encounters (`diagnosis_encounters` many-to-many), the documents store (dedup, orphan sweep, MIME allowlist, containment rules, agent access), CSV import/export, and the per-page UI inventory — in this file.
