---
paths:
  - "src/istota/location/**"
  - "src/istota/location_logic.py"
  - "src/istota/webhook_receiver.py"
  - "src/istota/garmin_routes.py"
---

# Location module

## GPS Location

Overland webhook → `webhook_receiver.py`. Asymmetric place detection (hysteresis on entry, continuous away on exit). Reconciler re-derives closed visits. Discovered clusters dismissable. Per-user `location.db` on **local disk** at `Config.module_db_path(user, "location")` (default `{db_path.parent}/modules/{user}/location.db`; moved off the FUSE mount so it runs WAL — see "Module DB storage" below) holds `location_pings`, `places`, `visits`, `location_state`, `dismissed_clusters` — module package at `src/istota/location/` with `resolve_for_user(user_id, config) -> LocationContext` (mirrors `feeds` / `money`). Skill CLI subcommands and web routes that need reverse-geocoding open a second connection to framework `istota.db` for the global `geocode_cache` / `reverse_geocode_cache` tables (cross-user Nominatim dedup); `location.db.with_geocode_conn(framework_db_path)` is the dual-conn entry point. One-time framework→per-user migrator at `python -m istota.location._migrate`, gated in Ansible by a `location_pings`-presence check.

**Silence is not departure, and the two halves used to disagree** (ISSUE-329, ISSUE-332). The live state machine already required an *observed* away ping to close a visit; `reconcile_visits` did not, and split a segment whenever `gap_sec > grace_sec` between two pings **at the same place** — so the reconciler undid the state machine's own rule on every pass, and a stationary tracker's quiet afternoon came back as four visits with the gaps missing. Same-place pings now extend one segment regardless of the interval; `reconcile_grace_minutes` still governs an *unassigned* ping, which is evidence of being somewhere else. That change lets a segment cross the rolling reconcile window, which the cleanup's `exited_at >= since AND entered_at < until` predicate did not cover — the pre-window fragment survived and overlapped the merged row — so the delete predicate gains a second arm keyed on the crossing segment's own `(place_id, first_ts, last_ts)`. The second half (ISSUE-332) is the same reasoning one layer up, in `geo.cluster_pings` — the stop clustering a **day summary** renders, not the `visits` table. The last ping at a stop is where the user was last seen, so `_finalize_cluster` now takes the next cluster's first ping (and, for the trailing cluster, an explicit `closing_ping`) and derives departure from it: haversine distance over the closing ping's recorded `speed`, or `DEFAULT_TRAVEL_SPEED_MPS` (13.4 m/s, ~30 mph) where it has none, clamped by `MAX_STOP_EXTENSION_SECONDS` (6h) so an overnight outage is not assigned to a stop. Both callers — `skills/location` and `web_app`, at `radius_m=250` — had to stop discarding `speed` on the way in, and both fetch that closing observation from **past the end of the requested day** while keeping it out of the day's own counts. There is one caller now: `location_logic.location_day_summary` is the single implementation and the two surfaces are thin wrappers over it, so a change to the closing-ping rule lands once. See `.claude/rules/skills.md` under the `location/` skill for what each surface still owns and why.

**Ping identity + replay.** `location_pings.client_id` (schema v3) is a per-point UUID the native client mints; a partial unique index (`WHERE client_id IS NOT NULL`) plus `INSERT OR IGNORE` makes a re-sent batch idempotent, so stock Overland and the Garmin importer — which send none — never collide. An empty-string `client_id` is coerced to NULL, since an empty string is a valid unique key and would collapse a device's whole history onto its first point. Dedup short-circuits **before** `_update_state_machine`, not merely before the row write: every ping at the current place increments the open visit's `ping_count`, which the reconciler and walk-by filter read, so a replay would inflate a visit that never grew. `insert_ping` returns `0` rather than a rowid on an ignored insert (`lastrowid` still holds the previous successful one, so it is not a substitute). The index is created in `_migrate_schema`, never `SCHEMA_SQL` — `init_db` runs `executescript` first, so on a v2 DB it would index a column the `ALTER TABLE` has not added yet.

**Declared points and altitude (ISSUE-229).** The shell's wifi-zone feature substitutes a *declared* coordinate while the device is on the configured home SSID, and fills the altitude slot with `-1` as its "unknown". `_process_feature` drops that altitude on the `wifi_zone` marker the shell already sends, **never on the value** — -1 m is a legitimate altitude, and matching on it would eat a real fix in Death Valley or most of the Netherlands. The marker itself is stored (`location_pings.wifi_zone`, schema v4) so a declared point stays identifiable afterwards, which is the reason the shell sends it; `vertical_accuracy` was dropped by the same omission and is stored beside it, with a negative value — iOS's documented "this altitude is invalid" signal — scrubbing the altitude the same way `speed` and `course` are already scrubbed. Rows written before v4 are swept once by `_backfill_declared_points`, which matches the declared fix's full signature (`altitude = -1` with a 1 m horizontal accuracy and a zeroed speed and course, Overland only) rather than the bare value. Nothing is substituted in the sentinel's place: a carried-forward last reading would draw a flat line asserting a measurement nobody took, which is the same defect one step politer. A home interval longer than the strip's gap threshold therefore draws the dashed connector the map draws above it; a shorter stint leaves a sub-threshold gap the strip still spans solid, which is the ordinary interpolation it does between any two consecutive samples rather than a claim about the interval.

**Provisioning.** `POST /api/settings/secrets/overland/ingest_token/generate` mints a token and returns it **once** — the only endpoint that puts a secret in a response body, because a token nobody sees cannot reach a phone. It refuses (503) when `ISTOTA_SECRET_KEY` is missing, and (409) when the location module is off for the user (the receiver builds no token map for them, so the token would be refused on first use) and when `[site] hostname` is unset (the assembled URL would be relative, which the QR decoder rejects for not being https; only reachable in no-auth mode, since Nextcloud auth already 403s a blank hostname at the origin check). Both checks run before minting, so a refusal never rotates a working token. `/location/settings` renders the token as a QR carrying a JSON envelope (`web/src/lib/location/provisioning.ts`) — deliberately not the webhook URL, which every generic scanner including iOS Camera would offer to _open_, putting the token in browser history. A write to the `overland`/`ingest_token` secret stamps a sentinel beside `istota.db` (`istota.location.ingest_signal`) that the receiver `os.stat`s once per ingest request, since the two run in different processes. `reload_config` reads the stamp _before_ loading secrets, so a token written mid-reload is not marked already-loaded, and concurrent threads claim the new stamp under a lock so one rebuild serves them all. Stamping is best-effort — a failure degrades to restart-only — and **`istota secret ensure` does not stamp at all**, so a CLI-provisioned token applies on the receiver's next restart or SIGHUP.

**Per-device tracker settings** live on the device and nowhere else — one account can have two phones, so a server-side setting would have them overwriting each other's row. The shell-gated "This device" card (`web/src/lib/components/location/DeviceTrackerCard.svelte`) talks to the `IstotaLocation` plugin through `web/src/lib/platform/nativeLocation.ts`, the capability facade (see `web/AGENTS.md` and the native-shell spec); in a browser it renders one stand-in line rather than nothing, so an absent section reads as deliberate. Both ingest sources — Overland and the shell's own tracker — post to `/webhooks/location`, authenticated per-user by the ingest token as a `?token=` query parameter (Overland) or an `Authorization: Bearer` header (the tracker); missing is 401, unknown 403. Exposed: on/off, profile (Detailed / Places), permission + a route into iOS Settings, tracking state, queue depth / last sent / last error, send now, rescan. Send interval and batch size are deliberately not exposed — the interval follows the profile. The Tracking row reads **Paused** (`--status-info-fg`, information rather than warning) when `TrackerStatus.paused` is set, with Stop still available because tracking is still armed — iOS keeps `tracking` true throughout a pause, so `paused` is not the inverse of it. The field is optional rather than version-gated: a shell older than 0.8.0 omits the key, and "absent reads as false" is exactly that shell's behaviour. A permission problem outranks a pause in the card's single warning slot — a pause resolves itself, while a missing authorization is why nothing is being logged at all, and showing both lands neither. The whole point of the card is making "is my tracker alive?" trustworthy, and "On" beside an empty queue and an hours-old last-sent is what a silently dead tracker looks like.

**Timezone following (ISSUE-096).** `scheduler.check_travel_timezone` runs on its own 15-minute clock (`TRAVEL_TZ_CHECK_INTERVAL`, gated on `config.location.enabled`) via `_spawn_background_check` — it opens every opted-in user's `location.db` and may notify, neither of which the dispatch thread should wait on, and the signal moves over hours so a per-minute sweep is pure churn. Detection is `location/timezone.py`, which writes nothing: `detect_travel_timezone` compares the newest usable ping against the newest usable ping at least `dwell_minutes` (60) older and returns a zone only when both resolve to the same one, the two sit within `settled_radius_m` (100 km) of each other, the track is fresh (< 2h), and the zone's wall clock differs from the stored zone's.

Four of those guards each fix a way the naive rule is wrong. **The radius, not the dwell, is what excludes a journey**: a continental flight spends over two hours above one zone, so a dwell-only rule sets the timezone to what is being flown over and then again on landing (`Etc/GMT±N` rejection only covers open water). **Timestamps are parsed, never string-compared** — `location_pings.timestamp` stores the client's string verbatim, so a device stamping `+02:00` sorts above a UTC bound and a text `WHERE` clause hides every recent row, silently disabling the feature for that device; `_recent_pings` reads and sorts through `garmin_import.parse_ts`, which carries the same warning. **Accuracy must be present**, not merely acceptable — an imported Garmin track has none, and failing open would let a backfilled activity anchor the decision. And **zones are compared by wall clock** (`same_wall_clock`), because `US/Pacific` and `America/Los_Angeles` are one zone under two names and Nextcloud seeds several such links.

Lookups are offline via `timezonefinder` (in the `location` extra), so no network call and no third party learns the position; a missing dependency logs one WARNING rather than failing silently, and no coordinates are ever logged. **Opt-in per user** (`user_profiles.timezone_follow_location`, default off; `istota user ensure --timezone-follow-location`, the Ansible per-user key, and the checkbox beside the timezone field in `/settings`) and always followed by a notification whose non-delivery is logged: it rewrites a value the user set, so it is a switch they throw rather than a behaviour they discover. The written zone is recorded in `istota_kv` (`location`/`auto_timezone`) and not re-written for 24h — detection is memoryless, so without that a user who sets it back by hand is overridden on every pass. Composes with ISSUE-102's seed-only rule: an auto-written zone is non-default, so `hydrate_user_configs` leaves it alone.

**Under `istota serve`** the receiver is not a separate process: `_maybe_mount_webhooks` _includes_ `webhook_receiver.location_router` into the web app and registers `reload_config` as a startup handler, so the endpoint is `/webhooks/location` on the **web** port and `webhooks_port` is unused. Mounting it as a sub-app instead produced `/webhooks/webhooks/location` and, worse, never ran the sub-app's lifespan — so the token map stayed empty and every correctly-tokened request 403'd. That shape has no SIGHUP handler, so the sentinel is its only reload path.

**Basemap tiles** come from `istota/map_basemap.py` and `[web.map]`, never from a URL in a component (ISSUE-334). `LocationMap.svelte` named `basemaps.cartocdn.com` twice as literals; CARTO began requiring a key and watermarking unauthenticated tiles, so every map surface here was defaced and no deployment could change it without a code edit. Two consumers read the one resolver — `GET /istota/api/map/basemap` and doctor's `web.basemap` — so the check cannot pass while the map is blank. A user's own stored CARTO key (`MODULE_SERVICE_SCHEMA["location"]["carto"]`, set from the location settings page) **selects CARTO for that user** and overrides the operator's `provider`, because otherwise pasting a key would do nothing visible and the reason would live in a file they cannot reach; the endpoint returns it only already embedded in the tile URL. Every failure — unknown provider, `custom` with no or a non-http(s) URL, a keyed provider with no key — falls back to `openfreemap` with `fell_back` and a reason, rather than returning keyless templates behind a flag no browser can act on. Full rationale in `.claude/rules/config.md` under `WebMapConfig`.
